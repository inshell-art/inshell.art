# Manual Adapter

Manual/direct minting remains permissionless. It requires no run ID, Plugin, callback, or Agent attestation. The caller supplies valid exact lines, conforming provenance, an exact registered spec pair, PATH authorization, and mint calldata.
